---
aliases:
context: "ML"
initials: 
isGlossaryTerm: true
sidebar: false
---



> [!primary] Younger Learners
> 

> [!secondary] Older Learners
> 
> When developing a classification model a label is given to a single piece of training data to identify which class the data belongs to. Once a model is trained new data can be provided to the model. The model will produce a prediction and a confidence score of which class the data belongs to.

> [!educator] Educators
> 
> In supervised learning, a machine learning ML  model is trained using labelled data. Each piece of data is annotated with one or more labels that provide information about that data. For example, an ML model is designed to identify bird sounds. Each sound is labelled with the name of the bird that made the sound. The ML model is trained with the labelled sounds and can predict the label (bird name) of new sounds. Data is most often labelled by people to provide accurate examples to train ML models with.